open OUnit2
open Sexplib
open Lambda


let inputs =
  [
  ]

let tests = List.map (fun (term,res) -> term >:: fun ctxt -> assert_equal (typed_to_simple_inTm (eval_inTm (read term))) (read res)) inputs     
